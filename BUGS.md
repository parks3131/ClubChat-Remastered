# Bugs

Every bug that reached a running app - the phone, the deployed API, the web build - one row each.
Newest first.

**This file is the index and stays short.** Read it whole in under a minute or it stops being read.
The detail lives in [`bugs/`](bugs/), one file per bug, opened only when you are chasing that one.

**Three columns, and the third is the point.** What broke, what fixed it, and what went wrong
*while* fixing it. Columns one and two can be reconstructed from the commit later. Column three -
the wrong first diagnosis, the test that should have caught it, the unrelated file that had to
change - cannot be recovered by anyone once the session is over.

| Bug | Fix | What went wrong while fixing |
|---|---|---|
| **2026-08-31** · **The rollback drill left production on the previous image, and said the wrong thing about why.** First real run, on `clubchat-worker`. flyctl upgraded ITSELF from 0.4.87 to 0.4.95 mid-run: `fly machine status` lost `--json`, so the readiness check parsed a usage message and declared a perfectly healthy rollback dead; and `fly machine update --image` began corrupting digest-pinned references into `repo@sha256:X@sha256:X`, so the roll-forward could not run. Every image here is digest-only, so no `--image` spelling survives. The worker ran the older image for seven minutes, draining normally. On the api or the gateway the readiness gate is `curl /ready` and would have passed, so the roll-forward alone would have failed and stranded live chat on a four-day-old image. [Detail](bugs/2026-08-31-the-tool-rewrote-itself-mid-drill.md) | The image swap goes through the Machines API with the machine's own config and one key replaced, so no CLI spells a reference. flyctl pinned for the run and its version printed. State read via `machine list --json`. Two latent defects fixed while there: the boot-line check now requires a NEW line rather than any line, and the final verification tolerates the third `.Config.Image` spelling. **Proved on all three apps the same day**, and the boot-line repair fired for real on the worker's first step: `state=started` with the previous boot's line already in the buffer, which is exactly the pair the old check accepted. The swap also blocks on the new `instance_id` reaching started, because returning on "update accepted" let the readiness probe be answered by the process being replaced. All of it proved on the worker at 19:17Z, where both guards fired on one run: `state=started` while the newest boot line was still the previous boot's, for 27 seconds. | **The drill's loudest line was a false accusation.** "THIS IS THE RESULT THE DRILL EXISTS TO FIND: the image you would roll back to does not serve traffic" - it served fine, and only the server's own log said so. Same shape as `restore-proof.mjs` calling four real DM channels orphans, four days earlier. I also reached for the production password first, which was refused and was the wrong instinct; running the drill inside the machine instead turned out impossible because the image carries no `scripts/`. A working command that printed no success message read as failed. The recovery I recommended (`fly deploy --image`) was safe on the worker only because `worker.toml` has no `release_command` - one app along it would have run a migration during a rollback recovery. And the alarm mailed twice for one event, still unexplained. **Worst of all: I then stranded the worker twice more with the code written to stop it being stranded** - a wait that asked for `timeout=120` against an endpoint capped at 60, and a boot-line counter that could go DOWN because `fly logs` is a rolling window, so the condition could never be met again. Both were syntax-checked, dry-run, and reasoned about at length in a comment, and neither was ever executed; one live call would have found either in seconds. The first of them went to `main` before it had run, on the strength of a green suite that cannot execute a shell script. |
| **2026-08-29** · **Closing a photograph left the Gallery with no way out.** Open a photo from a club's Gallery, close it, and the grid came back with no navigation bar at all - no title, no back arrow, tiles starting under the status bar. Reported off the iPhone with a screen recording an hour after the swipe feature went out over the air. The viewer had been told to hide the navigator's header so a photograph sits under the status bar; screen options reach the navigator through `setOptions`, which MERGES and persists, so unmounting the element that hid it was not an instruction to put it back. [Detail](bugs/2026-08-29-closing-a-photograph-left-no-way-out.md) | The grid branch states `headerShown: true` rather than relying on the absence of an instruction. Both branches now say what the header should be, and neither depends on the other having unmounted. | **I created it inside a fix and shipped it.** The header change was verified by opening a photograph and looking at it, which is the half that worked; closing it again was never tried, because closing was not what had been reported. The verification pass afterwards was broad and real - swipe direction both ways, the header following the sender across a sender boundary, Report appearing per photograph, 2,188 tests - and touched none of the close path. Nothing in the suite could have caught it either: the mobile app is tested as pure functions and nothing renders a navigator. There was also a cheaper answer visible beforehand and not taken - chat and Highlights draw the viewer OVER a headerless screen, so they need no option and have none to leave behind; the gallery replaces the grid instead, which is the only reason an option was needed. |
| **2026-08-29** · **A sheet came to rest inside the page rather than on the screen.** The DM three-dot menu drew as a card in the upper half with a large empty area under it and the dimming stopping where the card started. `position: absolute` with `bottom: 0` resolves against the nearest positioned ancestor, which on that screen is `Body` - a `ScrollView` - so the panel was placed against the scroller's CONTENT rather than the window. The identical component is correct on the screen next door, which renders it outside `Body`. Two other surfaces had it worse: the profile's club list and a club hub's races search were pushed below the fold entirely, behind the tab bar. [Detail](bugs/2026-08-29-a-sheet-came-to-rest-inside-the-page.md) | `SheetMenu` is a `RisingSheet`, and the two centred panels take a new shared `Overlay`. Both own a `Modal`, which has no container to be trapped by, so no caller can place them wrongly. The menu's chosen action now runs after the exit animation rather than unmounting the sheet from under itself. Also: the DM info header lost its "Chat info" title, leaving the back arrow and the three dots. | I fixed two screens nobody asked me to fix and had to be stopped - "Only do what I ask to do" - then reverted, asked as a multiple choice, and was told to do both. `headerTitle: () => null` does not remove a title and looks exactly like a bundle that did not reload: the native header is handed `title` separately, so an empty STRING is the lever. Making the races panel a modal put a context menu and two confirmations inside a modal, which iOS refuses in silence - the third time this project has walked into a rule it has written down. And three taps did nothing because an Apple system alert sat over the app, invisible to `simctl io screenshot`, which only photographs the device. |
| **2026-08-27** · **Your own bubble's tail pointed away from your own face.** Each bubble carries one small corner, and it reads as a tail: a received bubble's is top left, under the avatar in the left gutter. Your own was bottom left, pointing away from yours, which hangs in the right gutter. The corner never moved - the face did, when the author line moved above the bubble and mirrored earlier the same day. [Detail](bugs/2026-08-27-the-tail-pointed-away-from-the-face.md) | `styles.sent` is now the mirror of `styles.received`: small corner top right. Four lines, and the rule written beside both. | The report was misread twice, confidently: first as the pinned strip needing to jump to its message, which would have reversed a decision written down on 2026-08-11, then as the author line sitting wrong. Both came of decoding dictated words instead of asking what the drawn arrows meant. `sim.sh tap` also missed silently for a round - its mapping needs `_probe.png`, which only exists after a `shot` with no path, and the missing width fell through bash arithmetic as zero rather than as an error. And a drag to scroll registered as a long press, opening a menu with **Delete** in it on the founder's data. |
| **2026-08-27** · **Photos looked slow to load on every re-entry.** Three causes, none of them the CDN: the load is 0.4s, the loading placeholder was a hardcoded 240x240 square while a portrait photo draws 240x320 so the conversation jumped when it landed, and chat asked for the 1600px `display` variant to fill a 240pt slot. SQLite caches messages and has never held image bytes. [Detail](bugs/2026-08-27-a-photo-waited-in-a-square-hole.md) | `photoSize` owns the box for all three states. A `bubble` variant at 800px with a fallback table and a backfill. `expo-image` for a real memory and disk cache. Shipped server-first so production knew the name before any phone asked. Reached the founder's iPhone as build 1.0.0 (6) and was confirmed there the same day. | My opening theory was that the signed URL rotates so the cache can never hit - wrong, it is hour-aligned, and reading `api.ts` before proposing anything is the only reason it did not become the fix. Localhost was too fast to photograph the defect, so it needed an instrumented delay to hold the placeholder on screen. `expo-image` then crashed the app in dyld before any JavaScript ran, which turned up sixteen packages behind SDK 57 and turned a one-package addition into an SDK alignment and a new build. And `deriveVariants` named `thumb` and `display` literally in its skip check, so a third size would have made the backfill report success having written nothing. |
| **2026-08-27** · **Sending a message did not take you to it.** The list jumped once, the pinned strip vanished, and your own message sat under the composer with a **"1 new message"** pill over it. Nothing moved until you dragged. `send()` called `scrollToNewest()` one line BEFORE `sendWithRetry` created the row, so it scrolled to the message before yours; `maintainVisibleContentPosition` then held the viewport there while yours was inserted below the fold. [Detail](bugs/2026-08-27-the-send-scrolled-before-the-message-existed.md) | The intent is armed at the send and spent by an effect keyed on `rows`, once the row exists. Bounded re-application, and a finger always wins. Chat also now opens at the newest message rather than travelling to the first unread one, at the founder's request. | I spent the first pass planning a bubble-density redesign the videos happened to show, which he had not asked about. I then described the list settling to the bottom on its own after the ack - that was his finger, and the app never scrolled at all, which is the worse and simpler truth. Could not be reproduced in a browser (`maintainVisibleContentPosition` does not exist in react-native-web), and the Simulator build had to happen in a separate worktree because `pod install` moves the update fingerprint. |
| **2026-08-25** · **No address at `clubchatapp.com` could receive mail**, including `support@`, the contact published in the Privacy Policy, the Terms and the Profile screen, and required by App Review guideline 1.2. The apex MX pointed at the registrar's forwarding service, which only serves domains on the registrar's nameservers - this one is on Cloudflare's. Present, well formed, permanently inert. [Detail](bugs/2026-08-25-no-address-could-receive.md) | Cloudflare Email Routing on the apex, with rules for `support@` and `dmarc@`. `send.clubchatapp.com` untouched, so sending never broke. | A test message appearing under "to dmarc" was read as proof of delivery; Gmail shows your own sent mail regardless, and the bounce came later. The SPF swap had to delete before adding, or two `v=spf1` records would have meant no SPF at all. And `support@` returning 550 on all three nodes looked like a broken rule - it was propagation, caught by polling rather than by concluding. |
| **2026-08-25** · **Production error reporting had never worked.** All three `fly/*.toml` carried an invented Sentry DSN key - real project, fabricated key - so Sentry refused every event since launch with `403 with_reason: ProjectId`. [Detail](bugs/2026-08-25-production-reporting-never-worked.md) | The real DSN in all three tomls, redeployed. Plus `scripts/drills/sentry-ingest-check.mjs`, which posts one event and prints the status code. | Five layers reported success: `loadConfig`, the config-completeness check, `Sentry.init`, the transport, and `flush()` returning true. My first diagnosis blamed a missing DSN from a log line that logs every time by design; my second blamed a `beforeSend` added the same hour, which was innocent. Only a `fetch` to the ingest endpoint settled it, and there is no `curl` in the image. |
| **2026-08-25** · **Opening a meetup crashed the app**, minutes after a deploy. Migration 0041 dropped `meetups.location`, so the read stopped returning the KEY, and `DetailLine` guards `value === null` before calling `value.trim()`. Absent throws where null is handled. [Detail](bugs/2026-08-25-absent-is-not-null.md) | `location: null` and `mapPoint: null` as always-null compatibility keys; columns stay dropped. | Both sessions watched the deploy window (60s) instead of the client window, which never closes on its own. Two existing tests asserted the defect and would have locked it in. The checklist written to prevent a repeat missed `mapPoint`, because it is derived from `map_lat`/`map_lng` and its name is in no migration. |
| **2026-08-25** · Every **5xx since launch** reached Sentry with no `where` tag, no route, no method and no user id, so every 500 in production grouped into one issue. `@sentry/node` 10 captures Fastify errors itself before the app's handler runs, and Dedupe dropped ours as the duplicate. [Detail](bugs/2026-08-25-every-5xx-arrived-anonymous.md) | `Sentry.fastifyIntegration({ shouldHandleError: () => false })`. The SDK's tracing stays; only its error capture goes. Server-side, so no app rebuild. | Found by accident while enabling tracing, not by looking for it. Nothing failed: `capture` ran and returned an event id, and the symptom was an absence on a page nobody opens. The drill's first version printed `flush completed`, which reads as delivery and cannot mean it. Needed a real client with a real transport to assert at all. |
| **2026-08-25** · A nudge told a club **"18:00 at null"** with every form field filled in. The notification read `meetups.location`, uncollected since ADR-0037 ten days earlier, and `String(null)` is a valid string so nothing refused it. [Detail](bugs/2026-08-25-nudge-said-null.md) | Nudge reads `title` (`NOT NULL`). Dropped `location`, `map_lat`, `map_lng` - [ADR-0049](SPEC/decisions/0049-a-meetup-says-where-with-a-link-and-nothing-else.md). Server-side, so no app rebuild. | First diagnosis blamed the user for a field the form does not have; the proof was already in the screenshot. Claimed a resolver "was never built" after grepping a stale comment. `calendar.ts` had to change or every `/calendar` 500s. The test asserting no `"undefined"` passed throughout - now refuses `"null"` too. **The drop then crashed the shipped app** minutes after deploying: the read stopped returning the `location` *key*, and `DetailLine` guards `value === null` before calling `value.trim()`, so absent throws where null is handled. Broke [TECH/21](SPEC/TECH/21-deployment.md) rule 4 *and* rule 5. I had checked this and said it would "degrade gracefully" - from the comment above the call site, never having opened the component. |
